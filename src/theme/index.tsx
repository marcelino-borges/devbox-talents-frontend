import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#cc3539",
    },
    secondary: {
      main: "#edf2ff",
    },
  },
});

export default theme;
